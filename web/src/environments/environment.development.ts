export const environment = {
  production: false,
  // Use dev-server proxy to avoid CORS and survive backend restarts
  apiBaseUrl: '/api',
  apkUrl: '/downloads/giapha-android.apk',
  ipaUrl: '/downloads/giapha-ios.ipa',
};

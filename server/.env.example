PORT=3000

# MongoDB Atlas connection string.
# If the password contains special characters like @ : / ? # or spaces, URL-encode it first.
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/giapha?retryWrites=true&w=majority

# Optional: only set this if your MongoDB user authenticates against a specific database such as admin.
MONGODB_AUTH_SOURCE=
